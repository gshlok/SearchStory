{
  "version": 2,
  "builds": [
    {
      "src": "server/vercel-server.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/vercel-server.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/server/vercel-server.ts"
    }
  ]
}